<%= form_for [@restaurant, @review] do |form| %>
  
  <%= form.label :name, 'Author Name' %>
  <%= form.text_field :name %>

  <%= form.label :content, 'Review' %>
  <%= form.text_area :content %>

  <%= form.label :rating %>
  <%= form.select :rating, (1..10) %>

  <%= form.submit %>

<% end %>