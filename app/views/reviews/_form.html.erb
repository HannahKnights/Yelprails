<%= render 'error_form' %>

<%= form_for [@restaurant, @review] do |form| %>

  <%= form.label :content, 'Review' %>
  <%= form.text_area :content %>

  <%= form.label :rating %>
  <%= form.select :rating, (1..10), {:prompt => true} %>

  <%= form.submit %>

<% end %>